apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: devops-app
spec:
  type: LoadBalancer
  loadBalancerIP: "35.253.159.250"
  selector:
    app: api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  externalTrafficPolicy: Cluster